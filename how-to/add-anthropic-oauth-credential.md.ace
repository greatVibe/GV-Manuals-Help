file: add-anthropic-oauth-credential.md
consumers: [GreatVibe users, Mochi agents]
produces: [public Anthropic OAuth credential setup guide]
---
# Purpose
Shows how to open the secure credential wizard, add an Anthropic OAuth credential, and verify it without exposing tokens.
# Dependencies
- `add-ai-agent-credential.md`
# Gotchas
- OAuth values belong only in the credential wizard.
